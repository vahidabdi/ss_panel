mutation serviceType_New(
$name: String!,
$hasSubCat: Boolean,
$hasOperator: Boolean,
$nameEng: String!,
 ) {
  serviceType(
  name: $name,
  hasSubCat: $hasSubCat,
  hasOperator: $hasOperator,
  nameEng: $nameEng
  ) {
    id

  }
}
